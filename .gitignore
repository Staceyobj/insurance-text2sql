# Plan notes live in their own local-only repo (never pushed)
.plans/

# macOS
**/.DS_Store

# Python
.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/

# Environment (SPEC §8: .env never enters version control)
.env

# Generated eval artifact (regenerated by `make eval`; CI uploads it)
evals/report.md
